# LinkPresentationExample

iOS example for `@andreywtf/react-native-link-presentation`.

```sh
npm install
cd ios
pod install
open LinkPresentationExample.xcworkspace
```

The app demonstrates `LPMetadataProvider`, cancellation, custom metadata, both `LPLinkView` initializers, and lazy `NSItemProvider` file materialization.
